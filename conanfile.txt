[requires]
llvm-core/22.1.7
libxml2/[>=2.13 <2.14]
pybind11/3.0.1

[options]
# dwarf2cpp only reads object files and parses DWARF; it links LLVM libraries and needs
# no codegen backends, command-line tools, test utilities, SMT solver, or line editor.
# Trimming these drastically cuts the LLVM compilation units when the recipe is built
# from source (we publish the recipe only). zlib/zstd are kept so compressed debug
# sections can still be decoded.
llvm-core/*:targets=X86;AArch64
llvm-core/*:tools=False
llvm-core/*:utils=False
llvm-core/*:with_z3=False
llvm-core/*:with_libedit=False

[generators]
CMakeDeps
CMakeToolchain

[layout]
cmake_layout